<p><%= @resource.email %>さん</p>

<p>下のリンクからパスワードの変更が可能です。</p>

<p><%= link_to 'パスワードの変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>パスワードを変更しない場合は、このメールを無視してください。</p>
<p>上記リンクにアクセスしない限りパスワードは変更されません。</p>
